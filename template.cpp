#include <bits/stdc++.h>
using namespace std;

using ll = long long;

#define f(i,a,b) for(int i=int(a);i<int(b);i++)
#define fr(i,a,b) for(int i=int(a);i>int(b);i--)
#define test int TTT; cin >> TTT; while(TTT--)
#define pb push_back
#define pii pair<int,int>
#define vi vector<ll>
#define vvi vector<vector<ll>>
#define vcin(a,n) vector<ll> a(n); f(i,0,n) cin >> a[i];
#define vvcin(matrix, n, m) vvi matrix(n, vector<ll>(m)); f(i,0,n) f(j,0,m) cin >> matrix[i][j];
#define vpi vector<pair<int,int>>
#define mi map<int,int>
#define pll pair<ll,ll>
#define vll vector<ll>
#define vpll vector<pair<ll,ll>>
#define mll map<ll,ll>
#define SORT(x) sort(x.begin(),x.end())
#define SORTR(x) sort(x.begin(), x.end(), greater<>())
#define ALL(x) (x).begin(), (x).end()
#define YY cout << "Y\n"
#define NN cout << "N\n"
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL); cout << fixed << setprecision(10);

void io(){
    #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    #endif
}

int main() {
 
  // io();
  fast;
 
// Code



  return 0;
}
